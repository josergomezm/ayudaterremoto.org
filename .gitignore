# Logs
*.log
firebase-debug*.log*
firestore-debug*.log*
ui-debug*.log*

# Firebase
.firebase/
firebase/.firebase/
# Deploy flow copies app/dist here; it's generated.
firebase/app/

# Dependencies & build output
node_modules/
dist/

# Env (keep the example)
.env
.env.*
!.env.example

# Firebase Functions local secrets (e.g. GEMINI_API_KEY)
.secret.local

# OS / editor
.DS_Store
.claude/

# Native projects — generated by `npx cap add`. Remove these two lines once
# you start customizing native code and want it tracked in git.
app/ios/
app/android/

# Build outputs and cache
firebase/functions/lib/
*.tsbuildinfo
